DATABASE_URL="postgresql://metaagent:metaagent@localhost:5432/metaagent"

# Meta Marketing API (solo lectura, Nivel 1). Generar un token de "usuario
# de sistema" con permisos ads_read + read_insights en Business Settings.
# Nunca commitear el valor real — solo va en .env local o en las variables
# de entorno de Vercel.
META_ACCESS_TOKEN=""
META_AD_ACCOUNT_ID=""
